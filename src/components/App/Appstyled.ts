import styled from "styled-components";

const AppStyled = styled.main`
  padding-top: 100px;
  height: 100vh;

  .background-image {
    position: fixed;
    opacity: 0.5;
    z-index: -1;
  }
`;

export default AppStyled;
